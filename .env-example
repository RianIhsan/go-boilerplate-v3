# Server
SERVER_HOST=localhost
SERVER_PORT=8080
SERVER_READ_TIMEOUT=5
SERVER_WRITE_TIMEOUT=5
SERVER_MODE=Development
SERVER_SSL=false
SERVER_JWT_SECRET_KEY=
SERVER_ENCRYPT_KEY=

# Postgres
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DBNAME=
POSTGRES_SSL_MODE=disable

# Logger
LOGGER_LEVEL=info
LOGGER_CALLER=true
LOGGER_ENCODING=json
LOGGER_DEVELOPMENT=false

# Casbin
CASBIN_MODEL=config/casbin/casbin_auth_model.conf
CASBIN_POLICY=config/casbin/casbin_auth_policy.csv

# Google Drive
FOLDERIDDRIVE=
CREDPATHDRIVE=
LOCALHOST="http://localhost:8080"

# Redis
REDIS_ADDR=
REDIS_DB="0"
REDIS_PW=
PROVINCE_KEY_REDIS=

REDIS_POOL_SIZE=150
REDIS_MIN_IDLE_CONNS=30
REDIS_POOL_TIMEOUT=5
REDIS_CONN_MAX_IDLE_TIME=5
REDIS_CONN_MAX_LIFE_TIME=30
REDIS_READ_TIMEOUT=500
REDIS_WRITE_TIMEOUT=500

# Rate Limiter Configuration
# Global enable/disable flag
RATE_LIMITER_ENABLED=true

# API Endpoints - General usage
RATE_LIMIT_API_REQUESTS_PER_MINUTE=100
RATE_LIMIT_API_BURST=20

# Auth Endpoints - Login/Register (more restrictive)
RATE_LIMIT_AUTH_REQUESTS_PER_MINUTE=10
RATE_LIMIT_AUTH_BURST=3

# Device Endpoints - Device management
RATE_LIMIT_DEVICE_REQUESTS_PER_MINUTE=8000
RATE_LIMIT_DEVICE_BURST=300

# Admin Endpoints - Admin operations (more permissive)
RATE_LIMIT_ADMIN_REQUESTS_PER_MINUTE=200
RATE_LIMIT_ADMIN_BURST=30

# ClientEndpoint
RATE_LIMIT_CLIENT_REQUESTS_PER_MINUTE=50
RATE_LIMIT_CLIENT_BURST=10

# Minio
MINIO_ENDPOINT=
MINIO_ACCESS_KEY_ID=
MINIO_SECRET_ACCESS_KEY=
MINIO_USE_SSL=
MINIO_BUCKET_NAME=
MINIO_DEFAULT_AVATAR=

# SMTP
SMTP_HOST=
SMTP_PORT=
SMTP_EMAIL=
SMTP_PASSWORD=

# NATS
NATS_URL=
NATS_USERNAME=
NATS_PASSWORD=